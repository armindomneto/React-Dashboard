import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.dm7sistemas.minhacarteira',
  appName: 'minha-carteira',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
